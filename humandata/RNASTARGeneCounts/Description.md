The tabular files are the output of GeneCounts
The first column is the GeneID
The second column is the counts of paired reads
The third column is the counts of forward reads
The last column is the counts of reverse reads

For our purposes, we only require the first two columns
